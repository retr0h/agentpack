# Examples

## Manifest examples

How to write `agentpack.yaml` for different project layouts:

- [`manifests/single-plugin/`](manifests/single-plugin/) — standard layout with skills, commands, hooks, agents, MCP, settings
- [`manifests/multi-plugin/`](manifests/multi-plugin/) — monorepo with nonstandard directories, src/dest remapping

## Go library examples

Using agentpack packages programmatically:

- [`go/install/`](go/install/) — install a package from a git repo
- [`go/list/`](go/list/) — list installed packages
